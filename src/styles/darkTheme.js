import {
  darkThemeAccentColor,
  darkThemeGeneralColor,
  darkThemePrimaryColor,
  darkThemeTextColor
} from '../constants/styles.constatnts';

export const darkTheme = {
  palette: {
    primary: {
      bgColor: darkThemeGeneralColor,
      textColor: darkThemeTextColor,
      btnColor: darkThemePrimaryColor,
      hover: darkThemeAccentColor
    },
  },
};