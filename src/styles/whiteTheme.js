import {accentColor, generalWhiteColor, primaryColor, textColor} from '../constants/styles.constatnts';

export const whiteTheme = {
  palette: {
    primary: {
      bgColor: generalWhiteColor,
      textColor: textColor,
      btnColor: primaryColor,
      hover: accentColor,
    },
  },
};